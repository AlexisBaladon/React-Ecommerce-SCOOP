class ShowedImageInfo {
  title:string;
  pictureUrl: string;  
  
  constructor(title: string, pictureUrl: string) {
    this.title = title;
    this.pictureUrl = pictureUrl;
  }
}

export default ShowedImageInfo;
  