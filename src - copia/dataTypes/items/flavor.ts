class Flavor {
  id: string;
  title: string;
  pictureUrl: string;
  constructor(id: string, title: string, pictureUrl: string) {
    this.id = id;
    this.title = title;
    this.pictureUrl = pictureUrl;
  }
}

export default Flavor;